use gtk::prelude::*;
use gtk::{Application, ApplicationWindow};

fn main() {
    let app = Application::builder()
        .application_id("rmbu.ch")
        .build();

    app
    app.run();
}
fn buil_ui(app : &Application) {
    let window = ApplicationWindow::builder()
        .title("Test")
        .application(app)
        .build();

    window.show();
}